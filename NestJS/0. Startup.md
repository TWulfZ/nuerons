Agregar a la configuración de Eslint para evitar el error `Delete ␍` en Windows.
`.eslintrc.js`
```json
module.exports = {
  // ... other ESLint configurations
  rules: {
    'prettier/prettier': [
      'error',
      {
        'endOfLine': 'auto',
      },
    ],
  },
};
```